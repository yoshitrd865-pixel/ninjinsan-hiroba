# ひろば 環境変数サンプル
# .env にコピーして値を設定してください（縁側の .env とは完全に独立しています）

# セッション署名用シークレットキー
HIROBA_SECRET_KEY=change-me-to-a-random-secret

# データベース接続URL（未設定時は SQLite: hiroba.db を使用）
HIROBA_DATABASE_URL=sqlite:///./hiroba.db

# OpenAI APIキー（ボイスメモのWhisper音声認識に使用）
OPENAI_API_KEY=
